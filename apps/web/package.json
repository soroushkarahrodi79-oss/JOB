{
  "name": "@platform/web",
  "version": "0.0.0",
  "private": true,
  "description": "Next.js application host (GATE 2). Hosts the token layer, the self-hosted font, and the verification harness. Product screens are GATE 4.",
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@platform/domain": "workspace:*",
    "@platform/tokens": "workspace:*",
    "@platform/ui": "workspace:*",
    "next": "15.1.6",
    "react": "19.0.0",
    "react-dom": "19.0.0"
  }
}
